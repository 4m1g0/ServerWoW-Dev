From f6e56fdef37eb8aa86c35798cf9f029bfbf60ba5 Mon Sep 17 00:00:00 2001
From: 4m1g0 <casadelblanco@gmail.com>
Date: Thu, 12 Jan 2012 18:05:38 +0100
Subject: [PATCH 11/25] DisableChatNotifications

---
 src/server/game/Chat/Channels/Channel.cpp |    8 ++++----
 1 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/src/server/game/Chat/Channels/Channel.cpp b/src/server/game/Chat/Channels/Channel.cpp
index 37617bb..f6b7682 100755
--- a/src/server/game/Chat/Channels/Channel.cpp
+++ b/src/server/game/Chat/Channels/Channel.cpp
@@ -806,15 +806,15 @@ void Channel::MakeNotifyPacket(WorldPacket* data, uint8 notify_type)
 // done 0x00
 void Channel::MakeJoined(WorldPacket* data, uint64 guid)
 {
-    MakeNotifyPacket(data, CHAT_JOINED_NOTICE);
-    *data << uint64(guid);
+    /*MakeNotifyPacket(data, CHAT_JOINED_NOTICE);
+    *data << uint64(guid);*/
 }
 
 // done 0x01
 void Channel::MakeLeft(WorldPacket* data, uint64 guid)
 {
-    MakeNotifyPacket(data, CHAT_LEFT_NOTICE);
-    *data << uint64(guid);
+    /*MakeNotifyPacket(data, CHAT_LEFT_NOTICE);
+    *data << uint64(guid);*/
 }
 
 // done 0x02
-- 
1.7.5.4

